<div class="py-3">
    <h3>{{ book.title }}</h3>
    <div>
        <a href="{{ path('book_read', {id: book.id})}}" class="btn btn-primary">{{ book.title }}</a>
    </div>
    <p class="mt-2">
        {{ book.shortDescription }}
    </p>
</div>