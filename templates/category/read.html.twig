{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <section class="container py-5">
        <h1>{{ category.name}}</h1>
        <div>
            {% for book in books %}
                {{ include('category/book_preview.html.twig', {'book': book}) }}
            {% endfor %}
        </div>
    </section>
{% endblock %}