{% extends 'base.html.twig' %}


{% block body %}
    <section class="container py-5">
        <h1>{{ 'feedback'|trans }}</h1>
        <div>
            {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-success">{{ 'send'|trans }}</button>
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}
